{
  "name": "react-complete-guide",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "1.1.5"
  },

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "npx standard src && react-scripts test",
    "fix": "npx standard --fix src"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },

  "standard": {
    "env": [ "jest" ]
  },
  "devDependencies": {
    "standard": "^16.0.3"
  }
}
